{
  "name": "chat_js",
  "version": "0.1.0",
  "description": "Basic chat app using JS",
  "main": "chat_js.js",
  "scripts": {
    "pretest": "eslint --ignore-path .gitignore --ignore-pattern /public .",
    "test": "echo \"Error: no tests created yet\" && exit 0"
  },
  "author": "Cody Robertson",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "socket.io": "^2.1.1"
  }
}
